@code {
    [Inject]
    protected NavigationManager navigationManager { get; set; } = default!;

    [Inject]
    protected SupportedCards supportedCards { get; set; } = default!;

    [Inject]
    protected Decklist decklist { get; set; } = default!;
    [Inject]
    protected IFileSaver fileSaver { get; set; } = default!;
}